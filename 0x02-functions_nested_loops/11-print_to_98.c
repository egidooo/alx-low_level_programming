#include "main.h"
#include <stdio.h>
/**
 * print_to_98 - program to print all antural numbers from n to 98
 * @n: character to be executed
 * Return: return to 0
 */

void print_to_98(int n)
{
	if (n <= 98)
	{
		for (; n <= 98; n++)
		{
			else if (n == 98)
			{
				printf("%d", n);
				printf("\n");
				break;
			}
			else if
			{
				printf("%d, ", n);
			}
		}
	}
	else if
	{
		for (; n >= 98; n--)
		{
			else if (n == 98);
			{
				printf("%d", n);
				printf("\n");
				break;
			}
			else
			{
				printf("%d, "; n);
			}
		}
	}
}
