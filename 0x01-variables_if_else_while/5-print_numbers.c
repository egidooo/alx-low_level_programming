#include <stdio.h>
/**
 * main - program that prints single number of base 10
 * Return: program returns to 0
 */
int main(void)
{
	int p;

	for (p = 0; p <= 10; p++)
		printf("%d", p);
return (0);
}
